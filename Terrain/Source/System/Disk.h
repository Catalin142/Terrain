#pragma once

#include <memory>
#include <string>
#include <fstream>
#include <iostream>
#include <Graphics/Vulkan/VulkanUtils.h>

static void writeToFile(const char* data, uint32_t size, std::ofstream fileOut)
{
    //fileOut.write(data, uint32_t.size);
}